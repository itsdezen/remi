---
slug: tiktok-dj-club
status: active
path: /Users/itsdezen/Developer/tiktok-dj-club
updated: 2026-08-12
---

# tiktok-dj-club

## What it is
Three.js virtual DJ club, vertical (9:16) camera framing for TikTok Live. Viewers who join the live stream auto-spawn a matching avatar (gender auto-detected) into the club; they leave when the viewer leaves. A DJ character is always stationed at the booth. Viewers control their avatar via chat comments (numbered actions: jump, sway, etc.), shown in a guide panel top-right. Player actions (wings, spotlight, solo dance, ...) designed now; gifting-triggered premium unlocks are a later phase, not this build.

Stack: Vite (latest) + React + TypeScript, Three.js via `@react-three/fiber`/`@react-three/drei`, state via zustand. Club space design references two local clones for layout/lighting ideas (not code reuse — both are old vanilla-JS/global-script codebases, incompatible with an R3F component architecture): `Developer/clones/AMBRA_CLUB_THREEJS` (dance floor, spotlight rig) and `Developer/clones/Frogrey-Club` (UnrealBloomPass neon glow, audio-reactive lights, crowd-as-participants concept).

## Current focus
2026-08-12: **MVP built and committed** (`73573d9`) by a herdr sub-agent, verified (`tsc -b`, `npm run build`, `oxlint` all clean) and confirmed independently before closing the pane. Stack: Vite + React 19 + TypeScript, `@react-three/fiber`/`drei`/`postprocessing`, zustand. Vertical 9:16 canvas (`App.css`).

What exists: `src/scene/` (floor, elevated DJ booth, moving lights + sweeping spotlights, laser beams, disco accent, UnrealBloom-style post-processing, fake BPM pulse clock — drawing on `AMBRA_CLUB_THREEJS`'s booth/floor layout and `Frogrey-Club`'s bloom/light-rig approach) · `src/avatars/` (primitive low-poly humanoid, male/female palette variants, procedural animation state machine — idle-dance always on plus jump/sway/wave/spin/fistpump/watching/solo — permanent never-despawning DJ avatar, slot allocator so crowd avatars don't overlap) · `src/liveEvents/` (`LiveEventSource` interface + `MockLiveEventSource` simulating join/leave/numbered-comment traffic) · `src/gender/resolveGender.ts` (hash-based placeholder) · `src/actions/triggerAction.ts` (single chokepoint every action flows through, deliberately centralized for a future gift-gate wrapper) · `src/ui/GuidePanel.tsx` (top-right command legend). Commands 1–8: jump/sway/wave/spin/fistpump/wings/spotlight/solo. `README.md` has run instructions + a Roadmap section for what's explicitly out of scope this pass.

Not done this pass (by design, see README Roadmap): real TikTok Live ingestion (no public API — normal approach is an unofficial lib like `tiktok-live-connector` on a small Node backend relaying over WebSocket), real rigged GLTF avatar models (currently primitive geometry), gifting/premium gate on `triggerAction`, and a smarter gender signal. Visual in-browser smoke test wasn't completed (claude-in-chrome wasn't connected in the sub-agent's pane; declined installing Playwright+Chromium as a heavier substitute — build/lint/typecheck passing was judged sufficient for this pass). Worth an actual `npm run dev` + eyeball pass next session.

2026-08-12 (later same day): **Visual redesign pass** (`5a043de`), second herdr sub-agent, prompted by user feedback that the club looked plain and the camera wasn't actually letterboxed. Root cause of the letterbox bug: `App.css` `.stage` had both `width:100%` and `height:100%` alongside `aspect-ratio:9/16` — the explicit width+height canceled the ratio, so the canvas filled the whole browser window instead of a centered vertical strip. Fixed by driving off `height:100vh` with `max-width:100vw` only.

Visual overhaul referencing `AMBRA_CLUB_THREEJS/How_Looks.png` directly: procedural checkerboard floor (canvas texture, tuned roughness/metalness so spotlight pools read), bigger/brighter disco ball repositioned as hanging centerpiece, jagged tiered emissive backdrop wall behind the booth, 6-fixture overhead colored-light truss, orbiting lights converted to spotlights for crisper floor pools.

User also asked to port actual assets in ("màn hình lớn chạy video, bàn DJ có sẵn, các đèn chiếu") — researched both clones for real reusable assets: `Frogrey-Club/assets/bar_counter/scene.gltf` is a real CC-BY-4.0 DJ/bar-counter model (Sketchfab, "Bar Counter" by RimaAkter) — copied into `public/models/bar_counter/`, replacing the procedural booth riser, credited in README per license terms. `Frogrey-Club/assets/videos/*.mp4` turned out to be **unfetched Git LFS pointer stubs** (few KB of pointer text, not real video, ~150MB actual size never pulled) — did not fetch them from GitHub (that's an unrequested 150MB+ third-party download). Built a procedural canvas-visualizer "video screen" instead as a documented placeholder, one-file swap to a real `VideoTexture` later.

Known follow-ups: `scene.bin` (the GLTF binary) is 60MB — heavy for a page meant to run as a live-stream browser source, needs Draco/gltf-transform compression later. Console/turntable accent props on the booth are eyeballed against the GLTF's fixed height, not flush-fit to its actual bounding box (sub-agent couldn't get confident proportions pre-load). **Still no visual in-browser confirmation from either me or a sub-agent** — Chrome extension has not connected in this environment across multiple attempts (tried again after this pass too). User needs to eyeball `npm run dev` themselves at least once; flag this if picking the project back up.

2026-08-12 (third pass, same day): **AMBRA-fidelity pass + laser rework** (`3d83644`), third herdr sub-agent. User gave two directives: (1) copy AMBRA's actual space/models faithfully rather than just draw inspiration, (2) improve the laser rendering referencing both clones. Also mid-turn: "dùng model bàn DJ từ ambra thay vì frogrey" (use AMBRA's booth, not Frogrey's).

I read AMBRA's full `prot.html` (817 lines) directly to ground the brief in real numbers rather than re-deriving from a screenshot. Key finding: AMBRA_CLUB_THREEJS has **no LICENSE file at all** — briefed the sub-agent to reproduce composition/geometry/proportions procedurally, never copy its actual image assets, and specifically never use `ambra.jpg` (that's a real nightclub's actual logo/brand — "AMBRA CLUB / PROTECTOR PRESTIGE" — must never appear in our app). Also found AMBRA's booth is pure primitive geometry (no 3D model file), which resolved the earlier Frogrey GLTF licensing/60MB-weight concern by making it moot: removed `public/models/bar_counter/` (CC-BY Frogrey model, 61MB) and the README attribution section entirely, rebuilt the booth as primitives proportioned from AMBRA's real `draw()` values (console/legs/turntables/mixer/knobs).

Also ported: AMBRA's actual lighting choreography (6-7 independently-orbiting colored PointLights at varied radii/speeds/axis-lead + slower white "special" accent lights, replacing the smaller fixed set), a real reflective disco ball (drei `CubeCamera` envMap instead of flat emissive material), 3-sided boxed booth alcove. Added a low-priority extra: small beatClock-reactive EQ bar row (`EqBars.tsx`), inspired by AMBRA's real audio-FFT-driven bars but driven off our fake beat clock since we have no real audio.

Laser finding worth remembering: **Frogrey's `LaserBeam` class is dead code** — called throughout `js/tool.js` (grid emitters aimed at a shared moving point, red/blue gradients, reflection) but the class itself is never defined anywhere in that cloned repo (checked js/, js/components/, libs/ exhaustively). AMBRA has no beam geometry at all, just point lights. So the laser upgrade (`LaserBeams.tsx`) is original work, not ported: additive blending (`THREE.AdditiveBlending`) added to the beam material — this was the single biggest visual improvement — plus floor-landing glow decals, beams now emit from the 6 overhead truss fixture positions (shared via new `trussFixtures.ts`) each sweeping independently instead of one shared pivot.

**Real visual verification finally happened** — this sub-agent found a pre-existing cached Playwright+Chromium install on the machine (not a fresh download) and used headless screenshots to confirm the scene renders with no console errors. I reviewed the actual screenshots myself and published them as a Claude Artifact (🪩 favicon, "tiktok-dj-club — visual check") since the user's own Chrome extension still won't connect either — first time either of us has actually seen the app render.

Honest read of the screenshots (not just "looks fine" — actual issues spotted): the **disco ball reads oddly** — small, dark, oddly-faceted rather than a bright mirrored centerpiece, the CubeCamera reflection isn't landing well. The **booth/DJ figure is hard to read**, mostly a dark silhouette against the black backdrop, wants more fill/rim light. **Overall exposure is quite dark/moody** — fits a club aesthetic but may read as too murky on a phone screen for a TikTok Live overlay; worth a brightening pass. Flagged all three to the user, have not yet started a fix — next session should address these before considering the visual pass done.

Confirmed this wasn't a fluke: re-ran it live myself directly (found the same cached Playwright+Chromium the sub-agent used, at `~/Library/Caches/ms-playwright/chromium-1228/...`, launched via explicit `executablePath` since the default revision playwright's npm package expects wasn't cached) — same three issues reproduced. Published both the sub-agent's and my own live screenshots as a Claude Artifact (🪩 "tiktok-dj-club — visual check") so the user could see it too, since their Chrome extension also won't connect.

2026-08-12 (mega-session, same day): Long rapid-fire tuning session — user watched the dev server live and sent ~15 small follow-up requests in quick succession while sub-agents worked, several arriving mid-turn. Ran up to 3 parallel herdr sub-agents (panes split into a proper 3-wide top row, then back down as work finished): `dj-club-scene` (LightRig/Booth/LaserBeams/VideoScreen/layout.ts), `dj-club-avatars` (src/avatars/, public/models/), `dj-club-guide` (GuidePanel + actions.ts, finished quickly and closed).

**Workflow lesson learned, repeatedly**: `herdr agent prompt` sent to an agent that is `idle` sometimes leaves the text sitting unsent in the input box rather than submitting — confirmed this happened at least twice this session (a translation request and the LED-wall/floor-width requests both had to be resent). **Always verify the message was actually picked up** (check status transitions to `working`, or read the pane) rather than trusting the `agent_prompted` response alone; if it's still idle a beat later, send an explicit `Return` via send-keys. Also: backticks inside double-quoted shell strings passed to `herdr agent prompt` get interpreted as command substitution by the outer shell (bit twice — `radius`and `label` both got shell-evaluated and stripped) — avoid backticks in prompt text sent via Bash, or use a heredoc/file instead of inline strings.

**pnpm switch (`1643b76`) → dev server left running throughout this whole session** — every subsequent visual tweak landed live via Vite HMR while the user watched, per standing instruction "giữ nguyên dev server khi đang update để tôi check." Sub-agent briefs consistently included "don't touch the running dev server" and it held up well.

Commits from this session, roughly in order: `c0ddc97` light-speed+bloom, `1643b76` pnpm switch, `a71026f` widen camera, `f479f13` soften floor glow, `3eea982`+`295a741` guide panel compact grid + Vietnamese translation + drop heading, `5a043de`/`3d83644` (earlier passes, see above), `547b59b` fix booth-going-black (root cause was scene fog fully mixing distant materials to black regardless of light intensity — `fog={false}` on booth materials was the real fix, not just more lights), `fe0aded` 5-row ceiling LED grid + booth key/fill/rim lights, `b66de96` soften laser beam rays + enlarge floor glow pools, `0333463` richer radial-spectrum screen visual + 2 flanking portrait side-screens (referencing Frogrey-Club's screen concept, not AMBRA which has no comparable screen), `3f58fe4` **real GLTF avatar models** replacing primitives, `89eb4bc` encircling pulsing LED video wall + decoupled `FLOOR_WIDTH`(now 14)/`WALKABLE_WIDTH`(pinned at old 8.5, feeds `DANCE_FLOOR_BOUNDS` so avatar spawn area is unchanged), `4759bce` ring of fixed colored point lights around the dance floor perimeter.

**Avatar model licensing (important, don't relitigate)**: user downloaded 13 Sketchfab packs into `~/Downloads`. I checked every license — flagged and user confirmed **skip**: `genshin_impact_-_zhongli_chibi`, `goldship_bazooka_chibi`, `hishi_akebono_chibi`, `silence_suzuka_chibi`, `squid_game__pinksoldier_triangle` (fan-art of trademarked IP — Genshin/miHoYo, Uma Musume/Cygames, Squid Game/Netflix — infringement risk regardless of the modeler's own CC-BY license on their derivative sculpt), plus `running_boy` (CC-BY-**NC**) and `kgirls01` (CC-BY-**NC-ND**) — both legally blocked from commercial use outright. **Chosen and shipped**: male = `free_pack_-_cartoon_base_mesh` (DuNguyn, CC-BY-4.0, T-pose base mesh, no skeleton/animations — confirmed via GLTF `animations`/`skins` arrays being empty), female = `original_soft_and_stylized_girl_in_purple` (Kanna-Nakajima, CC-BY-4.0, baked PBR textures). Both wired into `Avatar.tsx` via `useGLTF`, keeping the existing procedural whole-body-root animation approach (no skeletal rigging exists to drive per-limb). Wings effect also swapped from procedural cones to a real GLTF model (`~/Downloads/wings`, xXsnakemanXx, CC-BY-4.0, also license-clean) — copied into `public/models/wings/`.

**Also added, uncommitted as of the account spend-limit interruption** (see below): floating username label above each avatar's head (drei `Text`/`Billboard`, small font, outline) using the `username` field already present on `AvatarState`/`Viewer` — this is explicitly a placeholder for the real TikTok display name once a real backend lands, per user's own framing.

**Session paused**: the `dj-club-avatars` sub-agent (herdr pane, was `p15`) hit the account's **monthly spend limit** ($27.11 remaining / $50.16 limit) mid-task, with the wings-model integration and username-label feature *uncommitted* (`src/avatars/Avatar.tsx` modified, `public/models/wings/` copied in but not `git add`ed). User chose to **wait for the natural reset** (3:50pm Asia/Saigon) rather than raise the limit or have me finish it directly. **Next session**: check `git status` in tiktok-dj-club first — if `Avatar.tsx` is still dirty with wings/username-label work, that's this interrupted task, safe to resume/finish (nothing was lost, just uncommitted). The scene sub-agent (`dj-club-scene`) finished cleanly with everything committed before the limit was hit.

**Still not resolved**: no real in-browser visual confirmation of this entire mega-session's combined result (LED wall + perimeter lights + 5-row ceiling grid + new avatars + wings + username labels + wider floor, all together) — every attempt at a live screenshot after the LED-wall/perimeter-lights changes has timed out (`page.goto` 30s timeout against localhost:5173, likely a slow HMR reload storm from so many rapid edits, or the scene got heavy enough to slow first paint). Worth a longer-timeout screenshot attempt or the user's own eyes before considering this batch done.

2026-08-12 (mega-session continued): Resolved with a `page.goto({waitUntil:'load'})` instead of `'networkidle'` (networkidle was hanging, likely due to the beat-clock/animation loop never going idle). Confirmed real screenshots after: 3-agent parallel run (`dj-club-scene`=lighting/screen, `dj-club-avatars`=Mixamo animations, `dj-club-ledgrid`=4-sided 30-fixture spotlight/laser grid) all landed — see commits `da8840c` (4-sided grid + ambient side lights, replacing the old single-row overhead truss), `d9fd9f5` (real skeletal Mixamo animations, `SkeletonUtils.clone()` + `AnimationMixer`, random per-avatar dance-clip crossfade cycling 5-11s).

**Key bug found via real screenshots (not code review) that both sub-agents missed**: `SkeletonUtils.clone()` leaves the clone's `matrixWorld` stale — measuring bounding box before an explicit `updateMatrixWorld(true)` reads garbage transforms, causing a ~100x scale error (avatars rendering huge/off-frame). Root-caused and fixed in `Avatar.tsx`.

**Second bug found via real screenshots**: booth region blew out to solid white after the new 4-sided LightGrid stacked with BoothLights' point lights (fixed in `7c6eeb7` — narrowed spotlight cones, halved BoothLights intensities, raised Bloom `luminanceThreshold`). **Third round, still not fully resolved as of this writing**: a separate bright blob persists at the dance-floor center even after that fix. Root-caused two contributing bugs myself (not delegated, direct fixes in `3506d26`): (1) `spotlight` avatar action was a permanent toggle with no expiry — random mock chat commands (`activateSpotlight` now, was `toggleSpotlight`) kept turning it on for different viewers with nothing ever turning old ones off, so intensity-12 point lights piled up over a session's runtime; now auto-expires after 5s via `clubStore.tick()`. (2) The angular center-out avatar placement fix (below) picked immediately-adjacent slots first on ties, visually overlapping now that avatars are full 3D models — reworked into an 8-sector angular round-robin so early spawns spread in different compass directions from center. Also toned down `VideoScreen.tsx`'s visualizer (near-white core/strokes/particles on an *unlit* `meshBasicMaterial` — external light tuning has zero effect on it, only its own drawn colors matter) to hue-tinted instead of pure white. **Still flagged open**: a bright spot near center persists in screenshots even after all three fixes — likely several independently-reasonable lighting systems (orbit lights, floor-ring lights, ambient side lights, grid spotlights) all moderately overlapping at the exact point avatars now spawn first, not one single clean remaining bug. Worth a fresh look next session with the user's own eyes, or a more surgical "reduce total light budget at floor-center" pass.

**Placement history** (`placement.ts`): row-major-from-booth-row (original) → naive squared-distance-from-center sort (`d3e0b69`-adjacent commit, caused adjacent-slot clustering) → 8-sector angular round-robin (`3506d26`, current) — if avatars ever look clustered again, check this file first, it's been touched three times today for the same underlying "how do slots get filled" concern.

**Cancelled**: user asked for an elevated runway-stage redesign + simplified action set (wings/jump/onstage-teleport/timed-white-theater-spotlight) then cancelled it before work started — not built, don't resurrect from memory of the request alone if picking this up later.

**Workflow notes reinforced this session**: (1) `page.goto({waitUntil:'networkidle'})` can hang indefinitely against this app (continuous animation loop never idles network) — use `'load'` instead for the Playwright verification scripts. (2) When Remi herself (not a sub-agent) needs to make a small single-file change, verify with `pnpm exec tsc --noEmit -p .` (fast, scoped) rather than spinning a sub-agent for genuinely trivial edits — did this directly several times this session (spawn-count tuning, removing dead lights, the spotlight/placement/screen bug hunt) alongside heavier sub-agent-delegated work running in parallel. (3) Hit the account's monthly spend limit mid-session once; user chose to wait for the natural reset rather than raise it — resumed cleanly afterward with no data loss (sub-agent's uncommitted work was still on disk).

2026-08-12 (mega-session, final entry — **paused here by explicit user request**, "dừng tất cả lại, commit hiện trạng cuối cùng"): Long unresolved thread on the dance-floor-center white blowout. Timeline of what was tried and ruled out, so the next session doesn't repeat the same guesses:
1. Booth-specific whiteout (separate, earlier issue) — **fixed** in `7c6eeb7` (narrowed LightGrid spotlight cones + halved BoothLights + raised Bloom threshold). Confirmed via screenshot at the time.
2. A *second*, still-open whiteout at floor-center (where avatars now spawn first per the center-out placement fix) — investigated directly by Remi (not delegated) across several rounds:
   - Ruled out: spotlight-action permanently stacking (fixed with 5s auto-expiry, `3506d26`) — real bug, real fix, but didn't touch this blowout.
   - Ruled out: avatar placement clustering (fixed with 8-sector angular spread, `3506d26`) — real bug, real fix, didn't touch this blowout either.
   - Ruled out: VideoScreen visualizer's near-white core/particles (recolored to hue-tinted, `3506d26`) — real improvement, unrelated to the floor blob.
   - User isolated it further: **male-only spawn → no blowout. Female present → blowout, scaling with female count.** Confirmed even after swapping in an entirely new female character (`e7f2efc`, regenerated from a fresh Mixamo dance set) — the *same* blowout persisted, meaning it's not a bug in one specific female asset.
   - Hypothesis (female's 7-mesh/126-node clothed+haired geometry catching more light than male's 3-mesh/79-node thin limbs) tested directly: dampened female-only material albedo by 84% (factor 0.16, `8a9d879`) — confirmed via console.log instrumentation that this genuinely applied (`materialsHit=7`) — **screenshot was pixel-identical before and after.** Conclusively rules out avatar material/albedo as the driver.
   - Also tried narrowing LightGrid's 15 spotlight cones (angle 0.46→0.26, intensity 7→3, decay 1.9→2.3, `8a9d879`) reasoning that each fixture's floor footprint radius (~6 units, `height*tan(angle)`) was large enough that all 15 overlapped at center regardless of aim-point spread — **also zero visible effect.**
   - **Net result: two significant, measured interventions (material albedo -84%, spotlight intensity -57% + narrower cones) both produced literally no visible change.** This is unusual enough to warrant suspicion that either (a) the light budget at that exact point is so extreme it's fully saturated regardless of any single contributor's reduction, or (b) something not yet identified/tested is the actual driver (worth checking: is the blob's screen position/size *exactly* the same across all these screenshots, i.e. truly static, which would itself be a clue; also worth checking EqBars.tsx, DiscoAccent.tsx, or a leftover/duplicate light source that wasn't touched by any of these edits).
3. **Next session should**: instrument a direct light-intensity probe at floor-center (e.g. temporarily add a debug plane with a material that visualizes incident light magnitude, or systematically disable each of the ~6 independent lighting systems — orbit lights, floor-ring lights, ambient side lights, booth lights, LightGrid spotlights, LightGrid lasers — one at a time via a quick toggle and re-screenshot each — to bisect which system(s) actually contribute to that specific blob) rather than adjusting individual systems' outputs by feel, which is what this session's later rounds did without success.

**Female character model swap** (unrelated to the whiteout root cause, but done this session): owner provided a fresh set of 8 Mixamo FBX exports in `~/Downloads/female/` (Dancing Maraschino Step, Head Spinning, Hip Hop Dancing ×4, Snake/Tut Hip Hop) — regenerated via the same `convert-dance-fbx.ts`/`run-convert-dance-fbx.mjs` pipeline built earlier this session, output committed as `e7f2efc`. The pipeline script gained an optional target-name CLI filter so a single gender can be rebuilt without touching the other's `.glb`. Male model (`~/Downloads/male/`, still the original set) untouched.

2026-08-12 (fourth pass): **Switched package manager npm → pnpm** (`1643b76`), fourth herdr sub-agent — small mechanical task (delete node_modules/package-lock.json, `pnpm install`, README instructions updated), build+lint verified clean. Per user request ("chạy dev server để tôi dùng thử"), left `pnpm dev` running detached in the background at `http://localhost:5173/` rather than killing it — user wants to open it in their own browser. pnpm binary lives at `~/.local/share/mise/installs/node/lts/bin/pnpm` (mise-managed, v11.5.2), not on PATH by default in a fresh shell.

2026-08-12 (fifth pass): **Light-speed + bloom-blur tuning** (`c0ddc97`), fifth herdr sub-agent — user watched the running dev server live and said the colored lights felt too slow/rigid ("quá cứng") and the glow too weak. Orbit-light speeds (`ORBIT_LIGHTS`/`SPECIAL_LIGHTS` in `LightRig.tsx`) roughly ×2.2, sweeping-spotlight/truss-flicker speeds ×1.5 (smaller bump, they were already faster). Bloom in `Club.tsx`: intensity 1.1→2, threshold 0.25→0.18, added `radius={0.7}`. Key workflow point: **the dev server from the pnpm-switch pass was kept running throughout** — sub-agent briefs explicitly said not to touch/restart it, so Vite HMR picked up each edit live while the user watched in their own browser. Worked cleanly; reuse this "leave the dev server running, just edit source" pattern for future live-tunable visual tweaks on this project instead of restarting each time.

## Key decisions / context
- Gender auto-detection has no real signal available client-side (no public TikTok profile/gender API) — sub-agent was told to build a pluggable `resolveGender(viewer)` interface with a placeholder heuristic (hash-based/round-robin), swappable later for a real classifier.
- Premium/gift-triggered unlocks are explicitly a later phase — this build only stubs the hook point (an action-trigger function actions can be gated behind later), no payment/gift integration.
- Project lives outside the `remi` repo at `/Users/itsdezen/Developer/tiktok-dj-club` (own git history, not managed by Remi's identity.md auto-push rule).

## Links
